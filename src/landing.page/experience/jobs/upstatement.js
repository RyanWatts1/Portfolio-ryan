import React from "react";
import { Title, Range, List, ListItem } from "./common";

const Upstatement = (props) => {
  return (
    <div>
      <Title {...props}></Title>
      <Range>
        <span>{props.range}</span>
      </Range>
      <List>
        <ListItem>
          Write Modern performant, mantainable code for a diverse array of
        </ListItem>
        <ListItem>
          Write Modern performant, mantainable code for a diverse array of Write
          Modern performant, mantainable code for a diverse array of Write
          Modern performant, mantainable code for a diverse array of
        </ListItem>
        <ListItem>
          Write Modern performant, mantainable code for a diverse array of Write
          Modern performant, mantainable code for a diverse array of Write
          Modern performant, mantainable code for a diverse array of Write
          Modern performant, mantainable code for a diverse array of Write
          Modern performant, mantainable code for a diverse array of
        </ListItem>
        <ListItem>
          Write Modern performant, mantainable code for a diverse array of
        </ListItem>
      </List>
    </div>
  );
};

export default Upstatement;
